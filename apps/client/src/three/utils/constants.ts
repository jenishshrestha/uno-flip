// ─── Scene constants ───

// Camera — above looking down with slight forward tilt (UNO mobile feel)
export const CAMERA_POSITION: [number, number, number] = [0, 12, 3];
export const CAMERA_FOV = 45;
export const CAMERA_NEAR = 0.1;
export const CAMERA_FAR = 50;

// Card dimensions (world units)
export const CARD_WIDTH = 1.0;
export const CARD_HEIGHT = 1.4;
export const CARD_DEPTH = 0.01;

// Card texture (canvas pixel dimensions)
export const CARD_TEX_WIDTH = 256;
export const CARD_TEX_HEIGHT = 384;

// Opponent display
export const OPPONENT_MAX_VISIBLE_CARDS = 5;
